자바스크립트 객체

	속성				재료
재료, 대상 + 처리하는 행위 -> 담은 자료 형 -> 객체

{} -> 객체

JSON(JavaScript Object Notation) 형식 -> 자바스크립트 객체 표기법
{
	속성 : 값,
	속성 : 값,
}

값 -> 원시값(숫자, 문자, null, undefined, true, false)
	-> 객체(원시값 제외 전부 - 함수 객체 -> 메서드)
	
속성 접근은 마침표로(객체명.속성, 객체명.메서드())