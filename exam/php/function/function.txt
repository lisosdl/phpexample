함수
	- 반복되는 작업을 하나의 코드로 묶어서 자동 처리하기 위함
	- 코드는 반복은 줄이는 것이 좋은 코드

function 함수명(인수1, 인수2, 인수3) {
	// 함수 처리 영역
	
	// 처리 결과를 후속 처리를 위해서 반환 할때
	return 반환값
}

function sum($a, $b)
{
	$c = $a + $b;
	return $c;
}

지역내에서 전역 변수를 쓸때
$GLOBALS